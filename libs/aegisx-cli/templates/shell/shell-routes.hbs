import { Routes } from '@angular/router';
import { {{PascalCase}}ShellComponent } from './{{kebabCase}}-shell.component';

/**
 * {{Title}} Feature Routes
 *
 * Main route structure:
 * /{{kebabCase}}                    -> Dashboard (default)
 * /{{kebabCase}}/dashboard          -> Dashboard module
 * /{{kebabCase}}/master-data        -> Master Data module (ax-launcher)
 *
 * CRUD modules are auto-registered by the generator when using --shell {{kebabCase}}
 */
export const {{PascalCase}}_ROUTES: Routes = [
  {
    path: '',
    component: {{PascalCase}}ShellComponent,
    children: [
      // Default redirect to dashboard
      {
        path: '',
        redirectTo: 'dashboard',
        pathMatch: 'full',
      },

      // Dashboard module
      {
        path: 'dashboard',
        loadChildren: () =>
          import('./modules/dashboard/dashboard.routes').then(
            (m) => m.DASHBOARD_ROUTES,
          ),
      },

      // Master Data module (ax-launcher for CRUD modules)
      {
        path: 'master-data',
        loadChildren: () =>
          import('./modules/master-data/master-data.routes').then(
            (m) => m.MASTER_DATA_ROUTES,
          ),
        data: {
          title: 'Master Data',
          description: 'Master data management modules',
        },
      },

      // === AUTO-GENERATED ROUTES START ===
      // CRUD modules will be auto-registered here by the generator
      // === AUTO-GENERATED ROUTES END ===
    ],
  },
];
