# PDF Charts - Quick Start Guide

> เริ่มต้นใช้งาน Charts ใน PDF ภายใน 5 นาที 🚀

## 🎯 เป้าหมาย

สร้าง PDF Report พร้อมกราฟแสดงข้อมูล โดยใช้เวลาน้อยที่สุด

---

## ⚡ Quick Start (3 Steps)

### Step 1: Import Services

```typescript
import { PDFMakeService } from '@/services/pdfmake.service';
```

### Step 2: เตรียมข้อมูล

```typescript
// ข้อมูลสำหรับตาราง
const data = [
  { product: 'สินค้า A', sales: 45000 },
  { product: 'สินค้า B', sales: 52000 },
  { product: 'สินค้า C', sales: 38000 },
];

// ข้อมูลสำหรับกราฟ
const chartData = {
  labels: ['สินค้า A', 'สินค้า B', 'สินค้า C'],
  datasets: [
    {
      label: 'ยอดขาย (บาท)',
      data: [45000, 52000, 38000],
    },
  ],
};
```

### Step 3: สร้าง PDF

```typescript
const pdfService = new PDFMakeService();

const pdfBuffer = await pdfService.generatePdf({
  title: 'รายงานยอดขาย',
  data: data,
  fields: [
    { key: 'product', label: 'สินค้า' },
    { key: 'sales', label: 'ยอดขาย', type: 'number' },
  ],
  charts: [
    {
      type: 'bar',
      position: 'before',
      data: chartData,
      options: {
        title: 'ยอดขายแยกตามสินค้า',
        colorScheme: 'primary',
        displayValues: true,
      },
    },
  ],
});

// บันทึกไฟล์
fs.writeFileSync('report.pdf', pdfBuffer);
```

**เท่านี้ก็เสร็จแล้ว!** ✅

---

## 📊 ประเภทกราฟที่รองรับ

### 1. Bar Chart (แท่งกราฟ)

```typescript
{
  type: 'bar',
  data: {
    labels: ['Jan', 'Feb', 'Mar'],
    datasets: [{ data: [100, 150, 120] }]
  }
}
```

**ใช้เมื่อไหร่**: เปรียบเทียบค่าระหว่างหมวดหมู่

### 2. Line Chart (เส้นกราฟ)

```typescript
{
  type: 'line',
  data: {
    labels: ['Jan', 'Feb', 'Mar'],
    datasets: [{ data: [100, 150, 120] }]
  }
}
```

**ใช้เมื่อไหร่**: แสดงแนวโน้มตามเวลา

### 3. Pie Chart (วงกลม)

```typescript
{
  type: 'pie',
  data: {
    labels: ['A', 'B', 'C'],
    datasets: [{ data: [40, 35, 25] }]
  }
}
```

**ใช้เมื่อไหร่**: แสดงสัดส่วน (%)

### 4. Doughnut Chart (โดนัท)

```typescript
{
  type: 'doughnut',
  data: {
    labels: ['A', 'B', 'C'],
    datasets: [{ data: [40, 35, 25] }]
  }
}
```

**ใช้เมื่อไหร่**: แสดงสัดส่วน พร้อมพื้นที่กลาง

---

## 🎨 การตั้งค่าพื้นฐาน

### ตำแหน่งกราฟ

```typescript
{
  position: 'before',  // วางก่อนตาราง (แนะนำ)
  position: 'after',   // วางหลังตาราง
  position: 'top',     // วางก่อน title
  position: 'bottom'   // วางท้ายเอกสาร
}
```

### สีกราฟ

```typescript
{
  colorScheme: 'primary',   // น้ำเงิน (เหมาะกับข้อมูลทั่วไป)
  colorScheme: 'success',   // เขียว (ผลลัพธ์ดี)
  colorScheme: 'warning',   // เหลือง/ส้ม (ต้องระวัง)
  colorScheme: 'danger',    // แดง (ปัญหา)
  colorScheme: 'mixed',     // หลายสี (ข้อมูลหลากหลาย)
}
```

### ขนาดกราฟ

```typescript
{
  width: 500,   // กว้าง (px) - แนะนำ: 400-600
  height: 300   // สูง (px) - แนะนำ: 250-400
}
```

### ตัวเลือกเพิ่มเติม

```typescript
{
  options: {
    title: 'ชื่อกราฟ',
    displayValues: true,    // แสดงค่าบนกราฟ
    showLegend: true,       // แสดง legend
    showGrid: true          // แสดง grid
  }
}
```

---

## 📝 ตัวอย่างการใช้งานจริง

### ตัวอย่าง 1: รายงานยอดขายรายสัปดาห์

```typescript
const pdfBuffer = await pdfService.generatePdf({
  title: 'รายงานยอดขายรายสัปดาห์',
  subtitle: 'มกราคม 2568',
  data: salesData,
  fields: [
    { key: 'week', label: 'สัปดาห์' },
    { key: 'sales', label: 'ยอดขาย (บาท)', type: 'number' },
  ],
  charts: [
    {
      type: 'bar',
      position: 'before',
      data: {
        labels: ['สัปดาห์ 1', 'สัปดาห์ 2', 'สัปดาห์ 3', 'สัปดาห์ 4'],
        datasets: [
          {
            label: 'ยอดขาย',
            data: [45000, 52000, 48000, 61000],
          },
        ],
      },
      options: {
        title: 'ยอดขายรายสัปดาห์',
        colorScheme: 'success',
        displayValues: true,
      },
      width: 500,
      height: 300,
    },
  ],
});
```

### ตัวอย่าง 2: รายงานสต็อกยาพร้อม 2 กราฟ

```typescript
const pdfBuffer = await pdfService.generatePdf({
  title: 'รายงานยาคงคลัง',
  data: stockData,
  fields: [...],
  charts: [
    // กราฟที่ 1: มูลค่าแยกตามคลัง
    {
      type: 'bar',
      position: 'before',
      data: {
        labels: ['คลังหลัก', 'คลังย่อย A', 'คลังย่อย B'],
        datasets: [{
          label: 'มูลค่า (บาท)',
          data: [800000, 450000, 250000]
        }]
      },
      options: {
        title: 'มูลค่ายาคงคลังแยกตามคลัง',
        colorScheme: 'primary',
        displayValues: true
      }
    },

    // กราฟที่ 2: สัดส่วนมูลค่า
    {
      type: 'pie',
      position: 'after',
      data: {
        labels: ['คลังหลัก', 'คลังย่อย A', 'คลังย่อย B'],
        datasets: [{
          data: [53, 30, 17] // เปอร์เซ็นต์
        }]
      },
      options: {
        title: 'สัดส่วนมูลค่ายาคงคลัง',
        colorScheme: 'mixed',
        displayValues: true
      },
      width: 400,
      height: 400
    }
  ]
});
```

---

## 🔥 Tips & Tricks

### ✅ DO (ควรทำ)

1. **ใช้ `displayValues: true`** - ช่วยให้อ่านค่าได้ง่าย
2. **จำกัดจำนวน categories** - ไม่เกิน 10 items ใน chart
3. **ใช้ colorScheme ที่เหมาะสม** - primary สำหรับข้อมูลทั่วไป
4. **ทดสอบกับข้อมูลจริง** - ก่อน deploy

### ❌ DON'T (ไม่ควรทำ)

1. **กราฟขนาดใหญ่เกินไป** - จะล้นหน้ากระดาษ
2. **ใส่ data เยอะเกินไป** - แนะนำไม่เกิน 50 จุด
3. **ใช้สีที่แตกต่างกันมากเกินไป** - อ่านยาก
4. **ลืม title** - chart ไม่มี context

---

## 🐛 แก้ปัญหาเบื้องต้น

### ปัญหา: ข้อความภาษาไทยแสดงเป็นกล่อง

**แก้**: ตรวจสอบว่ามี Sarabun fonts อยู่ใน `apps/api/src/assets/fonts/Sarabun/`

### ปัญหา: Chart ไม่แสดงใน PDF

**แก้**: ตรวจสอบว่าข้อมูล `data.labels` และ `data.datasets[0].data` มีจำนวนเท่ากัน

```typescript
// ✅ ถูกต้อง
labels: ['A', 'B', 'C'],
data: [10, 20, 30]

// ❌ ผิด (จำนวนไม่เท่ากัน)
labels: ['A', 'B', 'C'],
data: [10, 20]
```

### ปัญหา: PDF ขนาดใหญ่เกินไป

**แก้**: ลดขนาดกราฟ

```typescript
{
  width: 400,  // ลดจาก 600
  height: 250  // ลดจาก 400
}
```

---

## 📚 เอกสารเพิ่มเติม

- [PDF Template Charts Integration](./pdf-template-charts-integration.md) - คู่มือฉบับเต็ม
- [Chart Generation API](../../reference/api/chart-generation-api.md) - API Reference
- [PDF Charts User Guide](./pdf-charts-guide.md) - คู่มือใช้งานละเอียด

---

## 🎓 ตัวอย่างโค้ดสมบูรณ์

### ไฟล์ตัวอย่าง

```bash
# ดูตัวอย่างการใช้งานจริง
apps/api/src/services/__examples__/pdf-charts-example.ts
apps/api/src/services/__examples__/pdf-template-charts-usage.ts
```

### รันตัวอย่าง

```bash
# Generate example PDFs
npx ts-node apps/api/src/services/__examples__/pdf-charts-example.ts

# ไฟล์จะถูกสร้างที่
temp/test-charts/*.pdf
```

---

**เอกสารนี้อัพเดทล่าสุด**: 2025-12-19
**เวอร์ชัน**: 1.0.0

**หากมีคำถาม**: ดูเอกสารเพิ่มเติมหรือติดต่อทีมพัฒนา 👨‍💻
